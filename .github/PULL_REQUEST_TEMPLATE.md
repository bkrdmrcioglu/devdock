## What does this change?

## Why?

## How did you test it?

- [ ] Builds via `xcodegen generate && xcodebuild -scheme DevDock -configuration Debug build`
- [ ] Ran the app and exercised the changed feature manually
